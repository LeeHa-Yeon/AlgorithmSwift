import Foundation

let inputValue = readLine()

if let inputResult = inputValue {
    let array = inputResult.components(separatedBy: " ")
    let answer = Double(array[0])! / Double(array[1])!
    print(answer)
}
